{
  "name": "verifable-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --site public/",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4",
    "@cloudflare/vitest-pool-workers": "^0.4.29",
    "@cloudflare/workers-types": "^4.20240903.0",
    "typescript": "^5.6.2",
    "vitest": "1.5.3",
    "wrangler": "^3.75.0"
  }
}
